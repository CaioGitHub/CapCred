export const environment = {
  production: false,
  // Point to your local/dev gateway when it’s ready
  apiBaseUrl: 'http://localhost:3000',
  // In development, default to mock data until the gateway is ready
  useMocks: true,
};

